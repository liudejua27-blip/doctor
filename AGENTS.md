# 仓库协作规则

本文件约束此仓库中的人类与 AI 协作者。目标是让“文档 → 契约 → 实现 → 测试 → 发布证据”保持同一事实链。

## 开始任何任务前

按任务范围阅读：

1. `docs/00_DOCUMENTATION_SYSTEM.md`
2. `docs/15_GLOSSARY.md`
3. `docs/01_PRODUCT_REQUIREMENTS.md`
4. 对应领域文档（iOS、Agent、数据、API、3D、安全、隐私或测试）
5. `docs/decisions/` 中相关 ADR
6. `docs/14_TRACEABILITY_MATRIX.md`

不得只根据聊天内容或旧总体方案直接实现。

## 文档先于代码

- 新功能先创建或更新 Feature Spec、契约与测试计划，再写代码。
- 任何字段、状态、接口、权限、规则、用户确认或埋点语义变化，必须先改其规范真源。
- 代码与规范冲突时，不允许静默“以代码为准”；应暂停受影响发布，明确是修代码还是通过正式变更流程修规范。
- 未决问题必须带 ID、责任人、截止门禁，不得用实现中的默认值悄悄替代产品决策。
- `docs/README.md` 是唯一文档入口；2026-08-07 已退役的早期无 ID 总体方案不得作为实现依据，也不得重新复制回仓库形成第二真源。

## 不可破坏的安全约束

- 产品不诊断疾病、不输出疾病概率、不排除严重疾病、不处方、不替代医生。
- 2D/3D 标记只表达用户主观位置，不代表疼痛来源、受损组织或医学定位。
- 确定性红旗规则必须先于普通 LLM 分析运行；最高风险等级优先，LLM 不得降级。
- `NoRuleTriggered` 只能表示未命中当前规则，绝不等于“安全”。
- AI 提取内容先标为未确认；只有用户明确确认的事实才能写入正式身体档案。
- 分享、确认记录、覆盖已确认事实、读取额外敏感资料等动作必须经过服务端权限和/或用户确认。
- 模型、网络或验证失败时必须保留模板化安全入口和 2D 记录能力。
- 不得在普通分析、崩溃或遥测日志中记录原始健康对话。

## 开源与资产约束

- PydanticAI 使用固定发行版和最小依赖，不依赖 `main`，不调用私有 API。
- RehabMate 的 Three.js/HTML/CSS/GSAP 实现不得进入 iOS 生产架构；不得用 WKWebView 包装为正式方案。
- RehabMate 的最近椭球中心分区、`face.a` 区域判定、点击三态循环、默认“酸痛”和世界坐标针点不得复用。
- RehabMate 当前 `body.glb` 不得作为默认或专业生产模型；在许可作者链澄清前不得商用发布。
- 新增任何代码、模型、纹理、字体、数据集或内容来源时，必须更新第三方清单、许可证证据、哈希和修改说明。

## 完成一项功能的最低证据

- 需求 ID 与验收标准已存在。
- 设计、状态、失败/离线/无障碍路径已定义。
- OpenAPI/JSON Schema 与实现一致，兼容策略清楚。
- 单元、契约、集成、安全、无障碍和必要的真实设备测试通过。
- 与风险相关的功能已映射到 SafetyBaseline 和锁定测试。
- 文档链接、变更记录与追踪矩阵已更新。
- 没有将待临床审核的阈值或内容伪装为已批准事实。

## 改动风格

- 优先做最小、可验证、可回滚的变更。
- 保留用户已有改动；不要覆盖不相关文件。
- 稳定 ID 不复用、不改义；弃用使用迁移和兼容期。
- UI 状态、业务事实和 AI 推断必须分层，不能共用一个字段或枚举。
- 任何不确定映射要显式携带置信度并请求用户确认，不能静默猜测。
